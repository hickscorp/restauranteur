.medium-4.large-3.columns
  %nav.sidebar
    %ul.side-nav.sidebar
      %li.heading
        #{ I18n.t 'diets.title' }
        %a.fi-plus.tiny.round.success.button.right{ ng: { click: 'addDiet()', show: 'session.user.is("dietitian")' } }
      %li{ ng: { repeat: 'd in diets', class: '{ active: d.id==selection.diet.id }' } }
        %a{ ui: { sref: 'diets.show({dietId:{{d.id}}})' } } {{d.name}}

.medium-8.large-9.columns
  .row{ ui: { view: 'diet' } }
    .small-12.columns
      %p #{ I18n.t 'diets.no_selection' }
